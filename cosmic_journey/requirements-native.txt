# Simple Native Visualizer Requirements for Jetson TX1
# Python 3.5.2 compatible
# Uses tkinter (pre-installed, no compilation needed)

# Core dependencies
python-osc==1.7.4

# Note: tkinter is pre-installed with Python on Ubuntu 16.04
# No additional packages needed!

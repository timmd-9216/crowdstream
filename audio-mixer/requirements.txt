# Eurovision Music Mixing Engine - Requirements
# Core audio processing libraries
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# Audio manipulation and effects
pydub>=0.25.0

# Real-time audio and OSC control
# NOTE: Use system PyAudio on Raspberry Pi (apt install python3-pyaudio)
pyaudio>=0.2.14  # Commented: use system version 0.2.13 on RPi
python-osc

# Scientific computing and data processing
scikit-learn>=1.3.0
joblib>=1.3.0

# Numerical computation support
numba>=0.58.0
decorator>=4.4.0

# Audio file reading support
audioread>=3.0.0

# Time stretching and pitch shifting
soxr>=0.3.0
audiotsm>=0.1.2  # Fast real-time time-stretch (WSOLA algorithm, no system deps)
pyrubberband>=0.4.0  # High-quality time-stretch fallback (requires: brew install rubberband)

# Caching and utilities
pooch>=1.7.0
lazy-loader>=0.3.0

# Configuration and messaging
msgpack>=1.0.0
platformdirs>=4.0.0
requests>=2.31.0

# Threading and async support (built-in modules - listed for reference)
# threading - built-in
# queue - built-in
# multiprocessing - built-in

# Development and testing (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Optional dependencies for extended functionality
matplotlib>=3.7.0  # For visualizations
ipython>=8.0.0     # For interactive development
jupyter>=1.0.0     # For notebook development
